# 活动管理模块更新说明

## 更新日期
2025年10月18日

## 更新内容

### 1. 活动表新增字段

在活动（Activity）模型中新增了以下字段：

1. **活动编码（activity_code）**：自动生成的唯一编码（格式：ACT + 时间戳 + 4位随机数）
2. **活动类型（activity_type）**：下拉选择，选项有：
   - 付费陈列
   - 新网点开发
   - 消费联谊
3. **执行周期**：
   - 执行周期开始日期（execution_start_date）
   - 执行周期结束日期（execution_end_date）
4. **活动说明（description）**：原有字段，现作为活动说明使用
5. **是否需要申请参与（require_application）**：可选项，选项有：
   - 需要
   - 不需要
6. **参与客户范围（customer_scope）**：可选项，选项有：
   - 不限制
   - 终端客户
   - 经销商客户
7. **参与活动商品（product_scope）**：可选项，选项有：
   - 全部商品
   - 指定具体商品
   - 指定商品类型
8. **兑付方式（payment_method）**：可选项，选项有：
   - 仅现金
   - 仅商品
   - 现金或商品
   - 不限
9. **结案方式（settlement_method）**：可选项，选项有：
   - 单次结案
   - 月度结案
   - 季度结案
   - 半年度结案
10. **活动申请周期**：
    - 活动申请周期开始日期（application_start_date）
    - 活动申请周期结束日期（application_end_date）
11. **费用分摊比例（cost_share_ratio）**：输入比例额度（0-100%）
12. **客户签收（customer_signature）**：下拉选择，选项有：
    - 手写签收
    - 拍照签收

### 2. 活动中心列表页面更新

表头字段已更新为：

- 活动状态
- 活动名称
- 活动编码
- 活动类型
- 执行周期
- 活动申请周期
- 参与客户范围
- 客户申请数
- 客户参与数
- 创建人
- 操作

### 3. 新增活动申请表

新增了 `ActivityApplication` 模型，用于记录客户的活动申请信息，包括：
- 活动ID
- 申请客户名称
- 客户类型
- 客户ID
- 申请状态（pending/approved/rejected）
- 备注
- 申请人
- 创建时间
- 更新时间

### 4. 活动状态扩展

活动状态新增了"草稿"状态：
- draft（草稿）
- active（活跃）
- inactive（非活跃）

## 数据库迁移步骤

### 重要提示
在执行数据库迁移前，请务必备份现有数据库文件：`instance/deepvisit.db`

### 迁移步骤

1. **激活虚拟环境**
   ```bash
   conda activate deepvisit
   ```

2. **备份数据库**
   ```bash
   # 复制数据库文件到备份目录
   copy instance\deepvisit.db instance\deepvisit_backup_20251018.db
   ```

3. **执行迁移脚本**
   
   交互式迁移（推荐）：
   ```bash
   python migrate_activity.py
   ```
   
   自动确认迁移（用于自动化脚本）：
   ```bash
   python migrate_activity.py -y
   # 或
   python migrate_activity.py --auto-confirm
   ```
   
   强制重建表（警告：将删除所有活动数据）：
   ```bash
   python migrate_activity.py --force
   ```

4. **查看迁移结果**
   - 脚本会显示迁移过程和结果
   - 如果有现有活动数据，会自动生成活动编码
   - 新增的其他字段需要在后续手动填充

### 全新安装（无历史数据）

如果是全新安装或不需要保留历史数据，可以使用：

```bash
python init_db.py
```

这将删除所有现有表并创建新的表结构。

## 使用说明

### 创建活动

1. 进入"活动管理" -> "活动中心"
2. 点击"创建活动"按钮
3. 填写必填字段：
   - 活动名称（必填）
   - 活动类型（必填）
4. 根据需要填写其他可选字段
5. 点击"创建活动"保存

### 编辑活动

1. 在活动列表中点击"编辑"按钮
2. 修改活动信息
3. 可以修改活动状态（草稿/活跃/非活跃）
4. 点击"更新活动"保存

### 查看活动列表

活动中心列表现在显示所有状态的活动，包括：
- 草稿状态的活动（黄色标签）
- 活跃状态的活动（绿色标签）
- 非活跃状态的活动（灰色标签）

## 注意事项

1. **活动编码**：自动生成，无需手动输入，保证全局唯一
2. **日期字段**：使用日期选择器，格式为 YYYY-MM-DD
3. **费用分摊比例**：输入0-100之间的数值，支持小数
4. **客户申请数**：从 `ActivityApplication` 表中统计
5. **客户参与数**：从 `ActivityReport` 表中统计

## 兼容性说明

- 现有的活动上报记录（ActivityReport）功能保持不变
- 迁移后，现有的活动记录会保留原有数据并生成活动编码
- 新增字段对现有功能无影响，可以逐步完善数据

## 技术说明

### 修改的文件

1. **模型文件**
   - `app/models/activity.py` - 更新Activity模型，新增ActivityApplication模型

2. **路由文件**
   - `app/routes/activity.py` - 更新创建和编辑活动的处理逻辑

3. **模板文件**
   - `app/templates/activity/create_activity.html` - 更新创建活动表单
   - `app/templates/activity/edit_activity.html` - 更新编辑活动表单
   - `app/templates/activity/center.html` - 更新活动列表页面

4. **迁移脚本**
   - `migrate_activity.py` - 数据库迁移脚本

## 后续开发建议

1. **指定商品功能**：当选择"指定具体商品"或"指定商品类型"时，可以开发商品选择界面
2. **活动申请功能**：开发客户申请参与活动的前端界面和审批流程
3. **活动统计功能**：开发活动效果统计和报表功能
4. **活动模板功能**：支持创建活动模板，快速创建类似活动

## 问题反馈

如果在使用过程中遇到问题，请记录以下信息：
- 错误信息截图
- 操作步骤
- 数据库备份文件位置

