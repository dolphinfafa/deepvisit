# 数据上报模块说明

## 模块概述

数据上报模块是外勤管理系统的一个新增功能模块，用于管理和记录三种类型的业务数据上报：
1. **铺货上报** - 记录商品在客户处的铺货情况
2. **库存上报** - 记录客户的商品库存情况
3. **竞品上报** - 记录市场上竞争对手产品的信息

## 功能特性

### 1. 铺货上报
铺货上报用于记录商品在客户处的铺货情况，帮助企业了解产品的市场覆盖情况。

**字段说明：**
- 上报日期：铺货的日期
- 客户名称：铺货的客户
- 客户类型：终端客户/直营商/KOL
- 客户等级：客户的等级分类
- 客户经理：负责该客户的客户经理
- 商品编码：铺货商品的编码
- 商品名称：铺货商品的名称
- 规格：商品规格
- 商品类型：商品的分类
- 品牌：商品品牌
- 上报人：记录上报人员

**功能操作：**
- 新增铺货上报记录
- 编辑现有上报记录
- 删除上报记录
- 查看上报记录列表

### 2. 库存上报
库存上报用于记录客户的商品库存情况，帮助企业了解产品在客户处的库存状态。

**字段说明：**
- 客户名称：库存所在的客户
- 商品名称：库存商品的名称
- 规格：商品规格
- 商品编码：商品编码
- 库存数量：当前库存数量
- 备注：补充说明信息
- 上报人：记录上报人员
- 上报时间：上报的时间

**功能操作：**
- 新增库存上报记录
- 编辑现有上报记录
- 删除上报记录
- 查看上报记录列表

### 3. 竞品上报
竞品上报用于记录市场上竞争对手产品的信息，帮助企业了解竞争态势。

**字段说明：**
- 竞品名称：竞争对手产品的名称
- 商品名称：我方对应的商品名称（可选）
- 备注：竞品的详细信息，如特点、价格、市场表现等
- 上报人：记录上报人员
- 上报时间：上报的时间

**功能操作：**
- 新增竞品上报记录
- 编辑现有上报记录
- 删除上报记录
- 查看上报记录列表

## 技术实现

### 数据模型
模块定义了三个数据模型，分别对应三种上报类型：
- `DisplayReport` - 铺货上报模型
- `InventoryReport` - 库存上报模型
- `CompetitorReport` - 竞品上报模型

每个模型都包含：
- 自动生成的上报编码
- 完整的字段定义
- 创建时间和更新时间戳
- 与用户表的关联关系

### 路由设计
模块使用蓝图（Blueprint）架构，路由前缀为 `/data_report`：

**铺货上报路由：**
- GET `/data_report/display` - 铺货上报列表页
- GET/POST `/data_report/display/create` - 创建铺货上报
- GET/POST `/data_report/display/<id>/edit` - 编辑铺货上报
- POST `/data_report/display/<id>/delete` - 删除铺货上报

**库存上报路由：**
- GET `/data_report/inventory` - 库存上报列表页
- GET/POST `/data_report/inventory/create` - 创建库存上报
- GET/POST `/data_report/inventory/<id>/edit` - 编辑库存上报
- POST `/data_report/inventory/<id>/delete` - 删除库存上报

**竞品上报路由：**
- GET `/data_report/competitor` - 竞品上报列表页
- GET/POST `/data_report/competitor/create` - 创建竞品上报
- GET/POST `/data_report/competitor/<id>/edit` - 编辑竞品上报
- POST `/data_report/competitor/<id>/delete` - 删除竞品上报

**API接口：**
- GET `/data_report/api/products` - 获取商品列表
- GET `/data_report/api/customers` - 获取客户列表

### 模板文件
模块包含完整的前端模板文件：
- 各功能的列表页面
- 创建和编辑表单页面
- 删除确认模态框
- 数据联动功能（客户类型联动、商品信息自动填充）

## 使用指南

### 访问模块
1. 登录系统后，在左侧导航菜单中找到"数据上报"板块
2. 点击对应的菜单项进入相应功能：
   - 📦 铺货上报
   - 📊 库存上报
   - 🏆 竞品上报

### 创建上报记录
1. 在列表页面点击"+ 新增上报"按钮
2. 填写必填字段（标记为红色星号）
3. 对于铺货上报：
   - 先选择客户类型，系统会自动加载对应的客户列表
   - 选择客户后，客户等级和客户经理会自动填充
   - 选择商品后，商品详细信息会自动填充
4. 点击"提交"按钮保存记录

### 编辑和删除
- 点击列表中的"编辑"按钮可以修改上报记录
- 点击"删除"按钮会弹出确认对话框，确认后删除记录

## 数据库表结构

### display_reports (铺货上报表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| report_code | String(64) | 上报编码（唯一） |
| report_date | Date | 上报日期 |
| customer_name | String(128) | 客户名称 |
| customer_type | String(32) | 客户类型 |
| customer_level | String(32) | 客户等级 |
| customer_manager | String(128) | 客户经理 |
| product_code | String(64) | 商品编码 |
| product_name | String(128) | 商品名称 |
| specification | String(64) | 规格 |
| product_type | String(32) | 商品类型 |
| brand | String(64) | 品牌 |
| reported_by | Integer | 上报人ID（外键） |
| remark | Text | 备注 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### inventory_reports (库存上报表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| report_code | String(64) | 上报编码（唯一） |
| customer_name | String(128) | 客户名称 |
| product_name | String(128) | 商品名称 |
| specification | String(64) | 规格 |
| product_code | String(64) | 商品编码 |
| quantity | Integer | 库存数量 |
| remark | Text | 备注 |
| reported_by | Integer | 上报人ID（外键） |
| report_time | DateTime | 上报时间 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### competitor_reports (竞品上报表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| report_code | String(64) | 上报编码（唯一） |
| competitor_name | String(128) | 竞品名称 |
| product_name | String(128) | 商品名称（我方商品） |
| remark | Text | 备注 |
| reported_by | Integer | 上报人ID（外键） |
| report_time | DateTime | 上报时间 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

## 安装和部署

### 数据库迁移
如果是首次部署或升级到包含数据上报模块的版本，需要运行数据库迁移脚本：

```bash
# 激活conda虚拟环境
conda activate deepvisit

# 运行迁移脚本
python migrate_data_report.py
```

迁移脚本会自动创建三个数据表：
- display_reports（铺货上报）
- inventory_reports（库存上报）
- competitor_reports（竞品上报）

### 启动应用
```bash
# 确保在deepvisit虚拟环境中
conda activate deepvisit

# 启动应用
python run.py
```

应用启动后，访问 http://localhost:5000 即可使用数据上报功能。

## 文件清单

### 模型文件
- `app/models/data_report.py` - 数据上报模型定义

### 路由文件
- `app/routes/data_report.py` - 数据上报路由和业务逻辑

### 模板文件
- `app/templates/data_report/display_list.html` - 铺货上报列表
- `app/templates/data_report/create_display.html` - 创建铺货上报
- `app/templates/data_report/edit_display.html` - 编辑铺货上报
- `app/templates/data_report/inventory_list.html` - 库存上报列表
- `app/templates/data_report/create_inventory.html` - 创建库存上报
- `app/templates/data_report/edit_inventory.html` - 编辑库存上报
- `app/templates/data_report/competitor_list.html` - 竞品上报列表
- `app/templates/data_report/create_competitor.html` - 创建竞品上报
- `app/templates/data_report/edit_competitor.html` - 编辑竞品上报

### 迁移脚本
- `migrate_data_report.py` - 数据库迁移脚本

### 配置文件
- `app/__init__.py` - 已注册data_report蓝图
- `app/templates/base.html` - 已添加数据上报菜单项

## 注意事项

1. **权限管理**：当前所有登录用户都可以访问数据上报功能，如需要更细粒度的权限控制，需要进一步开发权限管理功能。

2. **数据验证**：前端和后端都进行了基本的数据验证，确保必填字段不为空。

3. **编码生成**：每条上报记录都会自动生成唯一的编码：
   - 铺货上报：DIS + 时间戳 + 随机数
   - 库存上报：INV + 时间戳 + 随机数
   - 竞品上报：CMP + 时间戳 + 随机数

4. **虚拟环境**：确保在conda的deepvisit虚拟环境中运行应用。

5. **数据关联**：上报记录通过外键关联到用户表（reported_by字段），记录上报人信息。

## 后续扩展建议

1. **数据统计**：可以添加数据统计和图表展示功能，分析铺货覆盖率、库存周转等指标。

2. **导出功能**：添加数据导出为Excel功能，方便数据分析和报告。

3. **照片上传**：为上报记录添加照片上传功能，提供更直观的现场证据。

4. **审批流程**：添加上报审批功能，让管理人员审核上报数据。

5. **批量操作**：支持批量导入和批量删除功能。

6. **数据联动**：与订单、库存等模块进行数据联动和交叉分析。

## 技术支持

如有问题或建议，请联系系统管理员。

