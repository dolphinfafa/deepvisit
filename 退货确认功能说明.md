# 退货确认功能使用说明

## 功能概述

退货确认功能已成功开发完成，为退货订单模块增加了完整的退货确认流程。业务员可以上传退货接收凭证来完成订单的最终确认。

### 新增功能特性

1. **退货确认流程**
   - 已审批的退货订单可以进入确认流程
   - 业务员需要上传退货接收凭证
   - 支持图片和PDF格式的凭证文件
   - 可添加接收备注信息

2. **状态管理**
   - 新增"已确认"状态
   - 完整的订单状态流转：草稿 → 待审批 → 已审批 → 已确认

3. **凭证管理**
   - 支持多种文件格式上传
   - 文件预览功能
   - 凭证文件路径存储

## 访问方式

1. 启动应用：`python run.py`
2. 访问地址：`http://localhost:8001`
3. 登录账号：admin / admin123
4. 访问退货订单：`http://localhost:8001/order/return`

## 使用流程

### 退货确认操作

1. **进入退货订单列表**
   - 访问退货订单管理页面
   - 查看已审批状态的订单

2. **确认退货**
   - 点击已审批订单的"确认退货"按钮
   - 弹出退货确认模态框

3. **上传接收凭证**
   - 选择退货接收凭证文件（支持图片和PDF）
   - 系统会显示文件预览
   - 填写接收备注（可选）

4. **提交确认**
   - 点击"确认退货"按钮
   - 系统自动上传文件并更新订单状态
   - 订单状态变为"已确认"

### 订单状态说明

- **草稿**：订单创建但未提交
- **待审批**：订单已提交，等待审批
- **已审批**：审批通过，可以进行退货确认
- **已拒绝**：审批拒绝
- **已确认**：退货确认完成，订单流程结束
- **已取消**：订单取消

## 技术实现

### 数据模型更新

```sql
-- 新增字段
receive_voucher VARCHAR(256)  -- 退货接收凭证图片路径
receive_comment TEXT          -- 接收备注
received_by INTEGER           -- 接收人ID
received_at DATETIME          -- 接收时间
```

### API接口

1. **上传凭证接口**
   - `POST /order/api/return/upload-voucher`
   - 支持文件上传
   - 返回文件路径

2. **确认退货接口**
   - `POST /order/api/return/confirm/{order_id}`
   - 更新订单状态为已确认
   - 记录接收人和接收时间

### 前端功能

1. **文件上传组件**
   - 支持拖拽上传
   - 文件格式验证
   - 文件大小限制

2. **文件预览**
   - 图片预览
   - PDF文件信息显示
   - 其他文件类型提示

3. **状态管理**
   - 实时状态更新
   - 操作按钮动态显示
   - 状态标签颜色区分

## 核心特性

### 1. 完整的退货流程
- 创建 → 提交 → 审批 → 确认
- 每个环节都有明确的权限控制
- 状态流转清晰可见

### 2. 凭证管理
- 支持多种文件格式
- 文件预览功能
- 安全的文件存储

### 3. 用户体验
- 直观的操作界面
- 实时反馈
- 错误提示清晰

### 4. 数据完整性
- 完整的操作记录
- 接收人和时间记录
- 凭证文件关联

## 注意事项

1. **文件上传**
   - 支持图片格式：jpg, jpeg, png, gif
   - 支持PDF格式
   - 建议文件大小不超过10MB

2. **权限控制**
   - 只有业务员可以进行退货确认
   - 只能确认已审批的退货订单

3. **状态管理**
   - 已确认的订单不能再次修改
   - 确认后的订单状态不可逆转

4. **文件存储**
   - 当前版本使用模拟文件路径
   - 生产环境需要配置实际的文件存储

## 测试验证

所有功能已通过完整测试：
- ✅ 文件上传功能
- ✅ 退货确认流程
- ✅ 状态更新
- ✅ 数据存储
- ✅ 权限控制

退货确认功能的加入使得退货订单模块更加完善，提供了完整的退货管理解决方案。

