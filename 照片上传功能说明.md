# 数据上报模块照片上传功能说明

## 功能概述

为铺货上报、库存上报、竞品上报三个模块添加了照片上传功能。用户可以在创建和编辑上报时上传图片文件。

## 已完成的修改

### 1. 数据库模型更新
- **文件**: `app/models/data_report.py`
- **更改**: 为 `DisplayReport`、`InventoryReport`、`CompetitorReport` 三个模型添加了 `photo` 字段（VARCHAR(255)）

### 2. 数据库迁移脚本
- **文件**: `migrate_add_photo_fields.py`
- **功能**: 为现有数据库添加photo字段

### 3. 路由更新
- **文件**: `app/routes/data_report.py`
- **更改**:
  - 添加文件上传辅助函数：`allowed_file()`, `save_uploaded_file()`, `delete_uploaded_file()`
  - 更新所有创建路由（`create_display`, `create_inventory`, `create_competitor`）以处理照片上传
  - 更新所有编辑路由（`edit_display`, `edit_inventory`, `edit_competitor`）以处理照片更新和旧照片删除

### 4. 模板更新

#### 创建表单
- `app/templates/data_report/create_display.html`
- `app/templates/data_report/create_inventory.html`
- `app/templates/data_report/create_competitor.html`

**更改**:
- 添加 `enctype="multipart/form-data"` 到表单
- 添加文件上传输入框
- 添加文件类型和大小提示

#### 编辑表单
- `app/templates/data_report/edit_display.html`
- `app/templates/data_report/edit_inventory.html`
- `app/templates/data_report/edit_competitor.html`

**更改**:
- 添加 `enctype="multipart/form-data"` 到表单
- 显示现有照片（如果有）
- 添加文件上传输入框以更新照片

## 使用说明

### 第一步：运行数据库迁移

在conda的deepvisit虚拟环境中运行以下命令：

```bash
# 激活conda环境
conda activate deepvisit

# 运行数据库迁移脚本
python migrate_add_photo_fields.py
```

### 第二步：启动应用程序

```bash
# 确保在deepvisit虚拟环境中
conda activate deepvisit

# 启动Flask应用
python run.py
```

### 第三步：测试功能

1. **登录系统**
   - 访问: http://localhost:5000
   - 使用管理员账号登录（用户名: admin, 密码: admin123）

2. **测试铺货上报照片上传**
   - 导航到：数据上报 > 铺货上报
   - 点击"新增"按钮
   - 填写必填字段
   - 在"上传照片"字段选择一张图片（支持JPG、PNG、GIF格式）
   - 提交表单
   - 返回列表，点击"编辑"查看是否显示上传的照片

3. **测试库存上报照片上传**
   - 导航到：数据上报 > 库存上报
   - 执行相同的测试步骤

4. **测试竞品上报照片上传**
   - 导航到：数据上报 > 竞品上报
   - 执行相同的测试步骤

## 技术细节

### 文件存储
- **存储路径**: `app/static/uploads/data_reports/`
- **文件命名**: `{prefix}_{8位随机字符}.{扩展名}`
  - 铺货上报: `display_xxxxxxxx.jpg`
  - 库存上报: `inventory_xxxxxxxx.jpg`
  - 竞品上报: `competitor_xxxxxxxx.jpg`

### 支持的文件格式
- JPG/JPEG
- PNG
- GIF

### 文件大小限制
- 最大 16MB（在 `config.py` 中配置）

### 文件访问
- 上传的照片通过Flask的static文件服务访问
- URL格式: `/static/uploads/data_reports/{filename}`

## 注意事项

1. **文件权限**: 确保 `app/static/uploads/data_reports/` 目录有写入权限
2. **磁盘空间**: 注意监控上传文件占用的磁盘空间
3. **安全性**: 
   - 文件类型验证已实现（仅允许图片格式）
   - 文件名经过安全处理（使用 `secure_filename`）
   - 使用UUID生成唯一文件名，避免文件名冲突
4. **删除机制**: 当更新照片时，旧照片会自动删除，避免文件累积

## 后续优化建议

1. **图片压缩**: 考虑添加图片自动压缩功能，减少存储空间
2. **缩略图**: 在列表页面显示缩略图
3. **多图上传**: 允许一次上传多张照片
4. **云存储**: 考虑使用云存储服务（如阿里云OSS、腾讯云COS）替代本地存储
5. **预览功能**: 在上传前添加图片预览功能
6. **删除功能**: 在编辑页面添加单独的删除照片按钮

## 故障排除

### 问题1: 上传失败
- 检查文件大小是否超过16MB
- 检查文件格式是否为JPG、PNG或GIF
- 检查uploads目录是否存在且有写入权限

### 问题2: 照片不显示
- 检查数据库中photo字段是否正确保存了路径
- 检查static目录下是否存在对应的图片文件
- 检查浏览器控制台是否有404错误

### 问题3: 数据库迁移失败
- 确保数据库文件存在于 `instance/deepvisit.db`
- 检查是否有其他程序正在使用数据库
- 查看迁移脚本的错误信息

## 开发环境要求

- Python 3.8+
- Conda虚拟环境：deepvisit
- 依赖包：
  - Flask
  - Flask-SQLAlchemy
  - Flask-Login
  - Werkzeug

所有依赖已在 `requirements.txt` 中定义。

