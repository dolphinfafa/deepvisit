# 活动上报模块更新说明

## 更新日期
2025-10-18

## 更新概述
本次更新对活动上报模块进行了全面改进，优化了表单结构和列表展示，增加了更多实用字段。

## 主要更新内容

### 1. 数据库模型更新 (ActivityReport)

#### 新增字段
- `payment_order_no` - 兑付单号（自动生成，格式：PAY+时间戳+4位随机数）
- `application_no` - 申请单号
- `customer_code` - 客户编码
- `address` - 详细地址
- `account_manager` - 客户经理
- `signature_method` - 签收方式（手写签收/拍照签收）
- `signature_photo` - 签收照片

### 2. 创建活动上报表单 (create_report.html)

按照新要求重新组织表单结构，包含以下6个主要部分：

1. **选择活动**
   - 活动下拉选择
   - 显示活动名称和活动编码

2. **选择客户（客户信息卡片）**
   - 客户类型（终端客户/直营商/KOL）
   - 选择客户（下拉）
   - 客户名称
   - 客户编码
   - 详细地址
   - 客户经理
   - 申请单号

3. **上传门店照片**
   - 支持JPG、PNG、GIF格式

4. **上传兑付照片**
   - 支持JPG、PNG、GIF格式

5. **上传活动陈列照片**
   - 支持JPG、PNG、GIF格式

6. **客户签收（签收信息卡片）**
   - 签收方式（手写签收/拍照签收）
   - 签收照片上传

### 3. 活动兑付列表页面 (reports.html)

#### 新表头字段（共14列）
1. 兑付单号
2. 活动名称
3. 活动编码
4. 申请单号
5. 客户名称
6. 客户编码
7. 详细地址
8. 执行周期
9. 客户经理
10. 兑付状态
11. 兑付照片
12. 签收方式
13. 签收照片
14. 操作

#### 优化内容
- 使用 `table-sm` 紧凑样式
- 照片缩略图调整为40x40像素
- 日期和地址使用 `<small>` 标签显示
- 状态使用 Bootstrap Badge 显示
- 空值统一显示为 "-"

### 4. 编辑活动上报页面 (edit_report.html)

按照创建页面的结构进行了重构，分为四个主要部分：

1. **基本信息卡片**
   - 兑付单号（只读）
   - 活动名称（只读）
   - 活动编码（只读）
   - 申请单号（可编辑）
   - 兑付状态（可编辑）

2. **客户信息卡片**
   - 客户类型
   - 客户ID
   - 客户名称
   - 客户编码
   - 详细地址
   - 客户经理

3. **照片信息卡片**
   - 门店照片（显示当前照片，可更新）
   - 兑付照片（显示当前照片，可更新）
   - 活动陈列照片（显示当前照片，可更新）

4. **客户签收卡片**
   - 签收方式
   - 签收照片（显示当前照片，可更新）

### 5. 路由处理更新 (routes/activity.py)

#### create_report 路由
- 添加了所有新字段的接收和处理
- 增加签收照片上传功能
- 文件保存到 `activity_signature` 文件夹

#### edit_report 路由
- 更新所有新字段的编辑功能
- 支持签收照片的更新

#### delete_report 路由
- 删除记录时同时删除签收照片文件

### 6. 数据库迁移

创建了 `migrate_activity_report.py` 迁移脚本：
- 自动添加7个新字段
- 为现有记录生成兑付单号
- 创建payment_order_no唯一索引

## 使用说明

### 运行迁移
```bash
cd F:\workspace\deepvisit
conda activate deepvisit
python migrate_activity_report.py
```

### 启动应用
```bash
cd F:\workspace\deepvisit
conda activate deepvisit
python run.py
```

## 技术细节

### 兑付单号生成规则
- 格式：`PAY + YYYYMMDDHHMMSS + 4位随机数`
- 示例：`PAY202510181430451234`
- 确保唯一性：通过数据库唯一索引保证

### 照片存储位置
- 门店照片：`static/uploads/activity_location/`
- 兑付照片：`static/uploads/activity_payment/`
- 陈列照片：`static/uploads/activity_display/`
- 签收照片：`static/uploads/activity_signature/`

### 字段映射关系

| 旧字段名 | 新字段名 | 说明 |
|---------|---------|-----|
| report_status | report_status | 状态改称"兑付状态" |
| - | payment_order_no | 新增：兑付单号 |
| - | application_no | 新增：申请单号 |
| - | customer_code | 新增：客户编码 |
| - | address | 新增：详细地址 |
| - | account_manager | 新增：客户经理 |
| - | signature_method | 新增：签收方式 |
| - | signature_photo | 新增：签收照片 |

## 注意事项

1. 数据库迁移后，所有现有记录会自动生成兑付单号
2. 所有新字段均为可选字段（除customer_name外）
3. 照片文件支持JPG、PNG、GIF格式
4. 编辑时如不上传新照片，则保留原照片
5. 删除记录会同时删除关联的照片文件

## 后续优化建议

1. 可以考虑从活动信息中自动填充执行周期
2. 可以从客户主数据自动获取客户编码和地址
3. 可以从用户信息自动填充客户经理
4. 可以添加批量导出功能
5. 可以添加兑付统计报表

## 版本信息

- 版本：v1.1
- 更新人：AI Assistant
- 更新日期：2025-10-18

